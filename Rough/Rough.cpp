#include <iostream> 
using namespace std;

int main() {
    int a = 5;
    int b = 21;
    if (a > b) {
        cout << (a) << (" is greater than ") << (b) << ("\n");

    }
    else if (a < b) {
        cout << (a) << (" is smaller than ") << (b) << ("\n");
    }
}

