#include <iostream>
using namespace std;

int main () {
    int a = 1;
    if (a>=0) {
        cout << (a) << " is positive\n";
    }else if (a<=-1) {
        cout << (a) << " is negative\n";
    }
    return 0;
}