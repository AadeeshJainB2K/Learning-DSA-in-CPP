#include <iostream>
using namespace std ;

int main () {
    int a,b;
    cout << "Enter The First Number ? ";
    cin >> (a);
    cout << "Enter The Second Number ? ";
    cin >> (b);
    cout << ("The sum of the given numbers is = ") << (a + b) << ("\n");
}